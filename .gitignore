# Node / React
node_modules/
dist/
build/

# Python / FastAPI
__pycache__/
*.pyc
.venv/
venv/

# Environment variables / secrets
# Every .env variant is ignored — a stray .env.bak or .env.old holds the same
# live credentials as the original. The committed example is the exception.
.env
.env.*
!.env.example
!.env.docker.example

# OS generated files
.DS_Store

landing.html
sample.html
card.txt