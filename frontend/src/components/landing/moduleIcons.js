/**
 * data/landing.js names its icons; this is the mapping to components.
 * Its own module so both the landing page and the feature modal can use it.
 */
import {
  ForumIcon, NewsIcon, MailIcon, DataRoomIcon, CommunitiesIcon,
  AnalyticsIcon, BoltIcon, ClockIcon, StackIcon,
} from '../icons/Icons'

export const ICONS = {
  forum: ForumIcon,
  news: NewsIcon,
  mail: MailIcon,
  dataroom: DataRoomIcon,
  communities: CommunitiesIcon,
  analytics: AnalyticsIcon,
  bolt: BoltIcon,
  clock: ClockIcon,
  stack: StackIcon,
}
